# 🧩 Paquetización y Métricas — Mini Proyecto

Este repositorio contiene un ejemplo simple de **paquetización de una aplicación Python** y la **generación de métricas** generales del proyecto.  
Incluye el código fuente principal (`main.py`) y una versión compilada para Android (`miaplicacion.apk`).

---

## 📁 Estructura del Proyecto

Paquetización y métricas/
│── main.py # Script principal de la aplicación
│── miaplicacion.apk # App generada/paquetizada para Android

---

## 🚀 Objetivo del Proyecto

El propósito de este mini-proyecto es:

- Demostrar cómo **paquetizar** una aplicación Python para ejecutarse fuera del entorno de desarrollo.
- Proveer un ejemplo funcional que pueda ser empaquetado en **Android (.apk)**.
- Analizar conceptos básicos de **métricas de software**, tales como:
  - Tamaño del proyecto.
  - Complejidad del código.
  - Componentes empaquetados.
  - Distribución de archivos.

---

## 📝 Archivo Principal: `main.py`

El archivo `main.py` contiene la lógica base de la aplicación.  
Se puede ejecutar directamente con:

```bash
python main.py
Asegúrate de tener instalado Python 3.10+.

📦 Paquetización
El proyecto fue empaquetizado en formato APK (Android).
El archivo resultante es:

Copiar código
miaplicacion.apk
Esto permite instalar la aplicación en cualquier dispositivo Android habilitando la opción “Instalar apps desconocidas”.

📊 Métricas del Proyecto
Las métricas consideradas dentro del alcance del mini-proyecto incluyen:

✔ Métrica de Tamaño
Número de archivos: 2

Líneas de código aproximadas (main.py): dependen del contenido

Tamaño total del proyecto comprimido: (según ZIP original)

✔ Métrica de Paquetes
1 script ejecutable (main.py)

1 paquete móvil instalable (miaplicacion.apk)

✔ Métrica de Distribución
Código fuente separado de la app compilada.

Estructura simple y clara.

🛠 Requisitos
Para ejecutar el proyecto desde el código fuente:

Python 3.x

Dependencias estándar de Python (si main.py las requiere)

Para instalar el APK:

Dispositivo Android 7.0 o superior

Permitir instalación de orígenes desconocidos

📥 Instalación del APK
Copia miaplicacion.apk a tu teléfono.

Activa Instalar apps desconocidas.

Abre el archivo y sigue las instrucciones.

🤝 Autor
Proyecto académico / demostrativo.